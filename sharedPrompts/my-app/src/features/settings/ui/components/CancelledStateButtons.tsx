import { RetryRequestButtons } from './RetryRequestButtons';
import type { RetryRequestButtonsProps } from './types';

export function CancelledStateButtons(props: RetryRequestButtonsProps) {
  return <RetryRequestButtons {...props} />;
}

