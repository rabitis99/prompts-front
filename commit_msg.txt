fix(prompt): resolve type mismatches, race conditions, and validation bugs

- Refactored `usePromptLike`, `usePromptFavorite`, `usePromptCopy`, and `usePromptActions` to use generics, fixing type safety issues with `setPrompt`.
- Added a `latestPromptIdRef` check in the error path of `usePromptLike` to prevent race conditions.
- Prevented state update race conditions in `useRelatedPrompts` by adding a `cancelled` flag on unmount.
- Enhanced `PromptCard` memo comparator to accurately check `description` existence and include event handler props (`onToggleLike`, `onCopy`).
- Updated `homeFeed.constants.ts` to derive `SortOption` types from keys to ensure synchronization.
- Added strict JSON validation in `useCreatePromptView` and `JsonSchemaStep` to block malformed schemas.
- Converted `setFormData` calls to use functional updaters in `CreatePromptView` to avoid state loss during batched updates.
- Improved accessibility by adding an `aria-label` to the textarea in `BodyStep`.